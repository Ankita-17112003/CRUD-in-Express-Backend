<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>User updation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: linear-gradient(135deg, #4facfe, #00f2fe);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      select {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 6px;
      }

      .form-container {
        background: #fff;
        padding: 30px 40px;
        border-radius: 10px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        width: 350px;
      }

      .form-container h2 {
        text-align: center;
        margin-bottom: 20px;
        color: #333;
      }

      .form-group {
        margin-bottom: 15px;
      }

      label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
        color: #555;
      }

      input[type="text"],
      input[type="email"],
      input[type="password"] {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 6px;
        font-size: 14px;
        transition: border 0.3s;
      }

      input:focus {
        border-color: #4facfe;
        outline: none;
      }

      .radio-group {
        display: flex;
        gap: 20px;
        align-items: center;
        margin-top: 5px;
      }

      .radio-group label {
        font-weight: normal;
        margin: 0;
      }

      .btn {
        width: 100%;
        padding: 12px;
        background: #4facfe;
        border: none;
        border-radius: 6px;
        color: #fff;
        font-size: 16px;
        cursor: pointer;
        transition: background 0.3s;
      }

      .btn:hover {
        background: #00c6ff;
      }
    </style>
  </head>
  <body>
    <div class="form-container">
      <h2>Update user data</h2>
      <form action="/updateuser/<%-data._id%>" method="POST">
        <div class="form-group">
          <label for="userName">Update Username</label>
          <input
            type="text"
            id="userName"
            value="<%-data.userName%>"
            name="userName"
            placeholder="Enter your username"
            required

          />
        </div>

        <div class="form-group">
          <label for="userEmail">UpdateEmail</label>
          <input
            type="email"
            id="userEmail"
            name="userEmail"
            value="<%-data.userEmail%>"
            placeholder="Enter your email"
            required
          />
        </div>

        <div class="form-group">
          <label for="userPassword">Update Password</label>
          <input
            type="password"
            id="userPassword"
            name="userPassword"
            value="<%-data.userPassword%>"
            placeholder="Enter your password"
            required
          />
        </div>

        <div class="form-group">
          <label for="userGender">update Gender</label>
          <select id="userGender" name="userGender" value="<%-data.userGender%>" required>
            <option value="">Select Gender</option>
            <option value="Male" <%-data.userGender==="Male"?"selected":""%>>Male</option>
            <option value="Female" <%-data.userGender==="Female"?"selected":""%>>Female</option>
            <option value="Other" <%-data.userGender==="Other"?"selected":""%>>Other</option>
          </select>
        </div>

        <div class="form-group">
          <label>Are you eligible?</label>
          <div class="radio-group">
            <input
              type="radio"
              id="eligibleYes"
              name="isEligible"
              value ="yes"
              <%= data.isEligible == 'yes' || data.isEligible == true ? 'checked' : '' %>
                                                                                           
              required
            />
            <label for="eligibleYes">Yes</label>

            <input type="radio" id="eligibleNo" name="isEligible" value="no" <%= data.isEligible == 'no' || data.isEligible == false ? 'checked' : '' %>/>
            <label for="eligibleNo">No</label>
          </div>
        </div>

        <button type="submit" class="btn">Update</button>
      </form>
    </div>
  </body>
</html>
